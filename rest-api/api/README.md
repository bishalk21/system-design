setup:

> `npm init -y` => creates a package.json file with default values.
> or
> `npm init` => creates a package.json file with custom values.
> `npm i express` => need express to create a server and handle requests and responses.
> `npm i nodemon --save-dev` or `npm i nodemon -D` => need nodemon to automatically restart the server when we make changes to the code.

create a file named `index.js` and add the following code:
